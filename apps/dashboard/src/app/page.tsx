/**
 * Dashboard shell landing. Auth, account, orders and returns arrive in
 * later passes — this page only proves the app boots and tests run.
 */
export default function DashboardHome() {
  return (
    <main>
      <h1>VEXXX Dashboard</h1>
      <p>Shell — customer account features land in later passes.</p>
    </main>
  );
}
