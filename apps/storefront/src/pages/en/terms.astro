---
import BaseLayout from '../../layouts/BaseLayout.astro';
import DocumentPage from '../../components/pages/DocumentPage.astro';
import { t } from '../../i18n';
import { routeAlternates, routePath } from '../../i18n/routes';

const locale = 'en' as const;
const dict = t(locale);
---

<BaseLayout
  locale={locale}
  title={dict.legal.terms.metaTitle}
  description={dict.legal.terms.metaDescription}
  path={routePath('terms', locale)}
  alternates={routeAlternates('terms')}
  noindex
>
  <DocumentPage eyebrow={dict.legal.eyebrow} heading={dict.legal.terms.heading} body={dict.legal.terms.body} status={dict.legal.pending} />
</BaseLayout>
