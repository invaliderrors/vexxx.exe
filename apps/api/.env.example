# @vexxx/api environment variables. Validated fail-fast in src/config/env.ts —
# add every new variable there and here in the same commit. Placeholders only,
# never real secrets.

NODE_ENV=development
PORT=3300
