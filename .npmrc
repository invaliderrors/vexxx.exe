# engine-strict deliberately off: transitive deps pin patch-level Node
# versions (e.g. sitemap@9 wants >=20.19.5). Our own floor lives in
# package.json engines and is enforced by CI's node-version.
