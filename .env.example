# vexxx environment variables
#
# The base needs none at runtime today — the canonical origin is pinned in
# astro.config.mjs (`site`). Document every new variable here in the same
# commit that introduces it, with a placeholder value, never a real secret.
